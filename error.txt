unbound local error

    def __init__(self, name, max_health, attack_power, mana):
        super().__init__(name, max_health, attack_power)
        def __init__(self, mana):
            self.mana = mana
        
 def __init__(self, mana):
    self.mana = mana

this is an innner function not the same as constructor. its basically def some_local_function(self, mana)
so we cant access self.mana here
